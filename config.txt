# Lower power consumption
dtparam=i2c_arm=off
dtparam=spi=off
arm_freq=800
arm_freq_max=800
arm_freq_min=300
gpu_freq=250
core_freq_min=250
sdram_freq_min=250
over_voltage=-6
over_voltage_min=-8
temp_limit=60

# Uncomment the next two lines if bluetooth or network not needed, which is
# recommended after final configuration of the system to avoid potential MIDI dropouts

#dtoverlay=disable-bt
#dtoverlay=disable-wifi

# Faster boot
initial_turbo=30
start_x=0
enable_uart=0
disable_splash=1
boot_delay=0